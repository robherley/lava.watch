#!/usr/bin/env bash
# script/test: run all tests + lints. Used by humans and CI.
set -euo pipefail
cd "$(dirname "$0")/.."

cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo test --workspace
